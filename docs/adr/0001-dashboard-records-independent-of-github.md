# Keep dashboard records independent of GitHub

The dashboard owns the records and statuses of ideas, projects, and features; v1 uses optional, manually maintained GitHub repository and issue links. This supports capturing work before a repository or issue exists and preserves abandoned projects after their repositories are deleted, while avoiding GitHub synchronization in v1. Status changes have no automatic effect on GitHub, and creating an issue remains a manual action.
